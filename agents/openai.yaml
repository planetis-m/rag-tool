interface:
  display_name: "Doc Assistant"
  short_description: "Chunk, label, store, and query chunkvec documents"
  default_prompt: "Use $doc-assistant to chunk this material into semantically coherent chunkvec input with stable topic labels, then prepare or run cvstore and cvquery as needed."
